package de.malte.event.enums;

/**
 * The Priority in which order the Listeners are going to be called.
 * Highest gets called first then High and so on.
 */
public enum Priority {
    LOWEST, LOW, DEFAULT, HIGH, HIGHEST
}
